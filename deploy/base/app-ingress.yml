kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "app-ingress"
  namespace: default

spec:
  rules:
    - host: ${TRAEFIK_HOSTNAME}
      http:
        paths:
          - path: /
            backend:
              serviceName: app-service
              servicePort: 80
